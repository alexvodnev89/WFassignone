<h1>Editing User</h1>

<%= render 'form' %>

<% if current_user && current_user.admin %>
    <div class="container">
        <%= link_to 'Show', @user %> 
        <%= link_to 'Back', users_path %>
    </div>
<% end %>

